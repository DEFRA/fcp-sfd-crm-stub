name: fcp-sfd-crm-stub-test

services:
  fcp-sfd-crm-stub:
    build:
      target: development
    command: npm test
    volumes:
      - ./test:/home/node/test
      - ./coverage:/home/node/coverage
